SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Banco — MVP usa SQLite (db.sqlite3 gerado automaticamente).
# Para PostgreSQL: USE_POSTGRES=True e preencha as vars abaixo.
USE_POSTGRES=False
DATABASE_NAME=syrax_db
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000
